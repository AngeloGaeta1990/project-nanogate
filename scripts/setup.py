from setuptools import find_packages, setup

setup(
    name = 'project-nanogate',
    packages = find_packages(),
    version = '0.0.1',
    description = 'Verify DNA sequence after assembly',
    author = 'AngeloGaeta90',
    license = 'MIT',
)
